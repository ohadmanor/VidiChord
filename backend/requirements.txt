# VidiChord backend dependencies.
#
# Python 3.11 or 3.12 is recommended. Everything here except madmom installs
# cleanly on 3.13/3.14 as well, but madmom has no wheels beyond 3.12 and does
# not build from source against NumPy 2.x. Without it the app still runs:
# beat tracking falls back to librosa and chord fusion uses two engines
# instead of three. See README.md.

# --- web layer ---
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.6

# --- audio acquisition ---
yt-dlp>=2024.4.9
requests>=2.31

# --- transcription ---
faster-whisper>=1.0

# --- audio analysis ---
librosa>=0.10
soundfile>=0.12
numpy>=1.26
scipy>=1.11

# Beat and downbeat tracking, plus the third chord engine.
# Optional: install on Python <=3.12. Needs Cython at build time.
madmom>=0.16.1; python_version < "3.13"

# --- development ---
# pytest>=8.0
